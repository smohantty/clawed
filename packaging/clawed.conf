# Clawed runtime configuration for Tizen TV
# Sourced by the clawed wrapper or systemd service

CLAWED_HOME=/opt/usr/data/clawed
CLAWED_SKILLS_DIR=/opt/usr/data/clawed/skills
CLAWED_LOG_DIR=/opt/usr/data/clawed/logs
